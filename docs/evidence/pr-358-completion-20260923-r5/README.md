# PR #358 aggregate budget and final dispatch correction

Hold source-correction budget, run, command and recovery authority through synchronous enqueue. Preserve durable retry and runner acknowledgement. Repair published-source replay projection and validate the same dated QA-root grammar before provisioning.

The final dispatch transaction rereads current authority after all lock waits and retains the aggregate, run and command locks until synchronous enqueue returns. A competing accounting transaction can either fence first and prevent enqueue or commit after enqueue. Disconnect leaves durable retry intact; sending does not fabricate a runner ACK. Async secret preparation remains outside that final transaction. Existing tenant, actor, recovery, lease, budget and source-authority checks remain enforced.

The final source tree is `452b82634081ab70c903db7da9d0702d2e6286fc`. All nine contributor gates passed on this tree. Native r10 rebuilt both binaries and passed 48 PostgreSQL regressions (15 retained-provider-receipt, 31 aggregate/store and two server tests), then failed before aggregate assertions because its external driver admitted only a September 22 fixture name. That failure and its exact driver are retained. Native r11 reused the hashed passing build/tests and the identical binaries, then freshly passed all three aggregate scopes, approval/rejection/expiry with runner ACK, and actual Edge/browser/server/runner policy acceptance on a fresh September 23 fixture. The r11 external changes corrected only the dated guard and the report output location; existing product assertions stayed intact. Six real product PNGs remain unchanged under `native/runtime-r11/`. `validation.png` and `acceptance.png` are captures of saved-result reports.

`dispatch/regressions.json` records actual owned-local execution of the three source-correction enqueue tests, 31 aggregate tests, correction authority, issue216 source-only publication, both issue221 exhausted retry cases, issue210 correction usage and issue169 exhausted resume. The store source matches the final tree. Only `crates/crony-server/src/main.rs` differs from that earlier regression tree; its final handling is covered by final-tree validation, native compilation and runtime acceptance.

`retrospective/` retains actual behavioral RED/GREEN: one aggregate fan-out failure at source `39632b957819012721c90902925d8fa7a9c7e873`, two independent lease-wait/reacquisition failures at `2e02c95a44fe4b1f7b103ac68c51e8c18476bca8`, and all three passing at candidate `e99301229873fcf1a68566715afe159f5bb3662f`. Historical product files are unchanged. Exact test-only patches, shared tests, separate callback adapters, source/test tree IDs and command logs are included. These are retrospective reproductions, not original development chronology. The candidate store bytes are identical to the final tree; the later server-only difference is explicit in `source-binding.json`.

To reproduce either historical baseline, use an isolated checkout at its reachable `source_commit`, apply its `test-only.patch` with `git apply --index`, and check `git write-tree` against `test_tree` in `retrospective/preparation.json`. The candidate preparation commit was local and is not required for replay: start from a fresh checkout of this published PR, remove only `docs/evidence/pr-358-completion-20260923-r5` from that disposable checkout's Git index with `git rm -r --cached -- docs/evidence/pr-358-completion-20260923-r5` (the evidence files remain on disk), apply `retrospective/current/from-final-source.patch` with `git apply --index`, and verify tree `049fbec36ff5b39084b052ff88e84bc6cbec3ea2`. Then apply the candidate's `test-only.patch` and verify tree `5e19038e482a4f512ee0856845b8bfeb157f7027`. This reconstructs the exact tested source without an unpublished object. For every case, provision a new owned PostgreSQL maintenance database and run `cargo test --locked -p crony-store pr358_retrospective_ -- --ignored --test-threads=1 --nocapture`. The baseline runs must fail the recorded assertions with exit101; the candidate must execute and pass all three. Never substitute a shared database or silently change historical product code.

## Published-source replay

The corrected first-run replay remains in `../pr-358-completion-20260922-r4/replay/`. Its private index reconstructs the source projection with only the selected evidence packet removed, checks the resulting tree identity, handles native Git errors, and restores caller index/environment. It requires no unpublished scratch tree. All three drivers admit the same `qa/pr265-run-activity-pr358-YYYYMMDD-rN` grammar, with checks before build/provisioning. Twenty-four source/admission guards passed, including a synthetic fresh clone lacking the recorded tree object; these tests started no services. Their script hashes still match this final source.

Use a fresh isolated checkout with frozen dependencies, PostgreSQL, Edge and Playwright; an unused dated QA root outside it; an owned Cargo cache; a new output directory; and no prior `target/debug/crony-server.exe`, `crony-runner.exe` or `output/e2e-approvals.json`. Invoke the corrected r9 replay with `-ValidationDirectory ./docs/evidence/pr-358-completion-20260923-r5`, the explicit `-Repository`, `-Revision`, `-QaRoot`, `-PostgresBin`, `-CargoTargetDirectory`, `-NodeDirectory`, `-PlaywrightModule` and `-OutputDirectory` arguments. The exact recorded r10/r11 drivers are supplied separately under `recorded-drivers/`. A complete fresh execution of the published replay is not claimed; r10/r11 execution and the 24 replay guard results are separate evidence lanes.

Local Node was 24.21.0; the repository pin is 24.19.0. Rust was 1.98.1 and PostgreSQL 17.10. Identity is developmental and providers deterministic. PostgreSQL used SCRAM; environment-only SQLx credential delivery remains reduced assurance. Server restart was explicitly skipped for this acceptance lane and is not claimed. Only exact owned processes were stopped; fixtures and prior results remain preserved. Hosted checks and required independent review remain separate gates.

Trailing line whitespace and blank lines at EOF are removed from the published logs. This formatting normalization does not alter test-result text; original and published hashes are distinct. The local `.gitattributes` preserves published evidence bytes on checkout.

The two standalone `pr358_retrospective_lease.rs` copies omit an extra blank line at EOF for publication. Their original and published hashes are recorded separately in `source-binding.json`. The unchanged `test-only.patch` files reconstruct the exact tested bytes and tree IDs; the original fixture files remain preserved.
