# Preserve evidence hashes across Windows checkouts and GitHub downloads.
# Accept CRLF evidence while retaining the usual whitespace checks.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# These frozen files include original output/source whitespace in their hashes.
candidate/gron.py -whitespace
official-score-summary.txt -whitespace
report.html -whitespace
