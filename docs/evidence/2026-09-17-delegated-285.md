# #285 implementation evidence — September 17, 2026

Branch: `feature/285-entra-local-obo`. Local mandatory validation is complete;
publication remains human-only. No factory, Base dependency, Azure provisioning, or remote
communication was used.

## Implementation-stage objective checks

Phoenix `sense` passed:

- Server tests: **145 passed, 4 intentionally ignored**. Includes 21 actual
  provider offline HTTP/JWT contracts and three broker helper tests.
- Trusted delegated runner tests: **11 passed**.
- Browser component tests: **14 passed**; synthetic fixture security: **5 passed**;
  existing local-stack lifecycle regressions passed.
- Server/runner all-target Clippy with `-D warnings`.
- `cargo fmt --check`.
- Web production build and lint.
- Immutable migration checker: **43 migrations**, prior checksums preserved.
- Integrated actual ECorp synthetic lane: **12 scenario assertions**, passed.
- Final `git diff --check`: passed.

Phoenix trace verification: **55 rows, no broken entry** at
`6693990239bb7b87030ec30fa58ed3cd1c03649096306bcd85553f5029231cc4`.
The trace and blessed snapshots remain under `.phoenix`; subsequent checks may
append rows. The first new broker checks were red before implementation.
The browser release-gate regression was red before the authorized-state fix.
The real browser initially reproduced HTTP 415 from mixed-case duplicate
Content-Type headers; a red test preceded the header normalization fix.
The repeated actual UI gesture then created a real job and automatically opened
the provider login form.
Initial runtime failure (incorrect runner revocation table assumption) is
retained as a failed actual mission rather than erased. Subsequent real
integration tests reproduce and pass the corrected assignment path.

## Actual integrated synthetic evidence

`output\delegated-evidence\synthetic-integration.json` records:

1. Protected resource denies anonymous access.
2. Original assignment cannot read before authentication.
3. Wrong task/run/connection epoch/assignment token denied.
4. Other human cannot authorize, cancel, release or inspect private preview.
5. Actual PKCE → Keycloak exchange → protected read → independent configured
   digest verification, with private human release gate still blocking worker.
6. One-use browser ticket and OAuth callback replay denied.
7. Same original run, normal native artifact/verifier completion; no resource
   value, client secret or human password appears in shared state.
8. Independent authenticated artifact download; downloaded SHA-256 matches
   signed artifact metadata and exact original task/run receipt.
9. Authenticated wrong provider subject denied.
10. Cancellation denies read and unopened browser ticket.
11. Expired operation cannot read or reopen authorization.
12. Expired downstream grant cannot read or release (explicit test-admin clock
    fault, not a production endpoint).

The passing synthetic mission is `96b3a8f6-7451-49cf-913a-ea4eafc47759`;
task `060b1616-ec86-44f4-9117-0580eec387a5`;
original run `85d4a41a-e71c-4156-897b-1e7f521fcf5c`.
Its downloaded receipt is
`output\delegated-evidence\synthetic-receipt.json`.
These tests use a distinct headless Edge session and synthetic credentials.
**They are not the requested human acceptance.**

## Human report and independent persisted verification

The user reported the human demonstration worked. Read-only verification at
**2026-09-17 21:53:40 UTC** found a later completed job, distinct from the
previously supplied task. This is local acceptance of the bounded Keycloak
connector, not evidence that every earlier attempt succeeded:

| Identity | Verified completed job |
| --- | --- |
| Mission | `b868a483-aa3a-4895-9c9c-9645b94ef8b6` |
| Task | `4b2e4ec3-2db8-423e-944e-6b47da7a00e8` |
| Original run (only run for this task) | `6f8c0f21-b06c-45fa-8d8f-b19d0e967eb0` |
| Private operation | `27e60d15-aac8-4d0a-b804-203148326283` |
| Artifact | `f24de4be-0b6d-44f9-8e3d-157fda54e266` |

Mission, task and run are **completed**; persisted run verification is **passed**;
operation is **completed**, `released=true`. Independent actor-authenticated
download used the existing `run.artifact_uri` path from `tools\e2e_delegated.mjs`:

`http://127.0.0.1:8791/api/corps/00000000-0000-4000-8000-000000000001/artifacts/f24de4be-0b6d-44f9-8e3d-157fda54e266?actor_id=00000000-0000-4000-8000-000000000011`

The 349 downloaded bytes hash to
`649961e4e2ec415903b570197b4afb9f86648cf1997751455cc7606f877089c3`,
matching the signed artifact metadata. Receipt operation/task/run IDs match
exactly; `verified=true`, `subject_preserved=true`; its resource digest matches
the private preview. Safe local readback evidence is retained in
`output\delegated-evidence\human-completion.json` and `human-receipt.json`.
The cleanup agent did not log in, click consent, release a receipt or create a
replacement task. The user's report supplies human attribution; development
principal state alone cannot independently prove which person clicked.

**Earlier failures remain failures.** The supplied mission
`2008c894-f141-4564-a1a6-fded2d3b07fc`, task
`2f577b7c-55c8-4eba-b23e-729fa4156bd1`, run
`597e29d4-82ba-400d-b6e9-0e0c183828f4` persisted **failed**, verification
**pending**, no artifact, operation **expired**, `released=false`.
The earlier handoff task `8581585a-b60d-46e5-8508-520182931818` also expired
unreleased. Historical handoff screenshots/URLs are retained privately; they
are not current sign-in instructions or proof of completion.

**Runtime/source distinction:** the preserved demo database reports latest
applied migration **42**. Migration 43's decision-audit table is not installed
there, so no retrospective transition/audit-history acceptance is claimed.
All 43 migrations and the post-review atomic admission change were exercised
in the SQLx-owned disposable regression database, not deployed to this running
demo. API/UI/runner owned processes were left running without restart.

## Cleanup validation and historical commit blocker

Final Phoenix checks passed:

- Server: **145 passed, 5 ignored** (four prior opt-in tests plus the new opt-in
  room-admission regression).
- New actual-migration room-admission test: **1 passed** with an explicitly
  owned SQLx maintenance database; a noncanonical room rolls back all six
  mission/task/staffing/actor/event/operation counts; canonical admission
  atomically persists the matching operation.
- Delegated runner: **11 passed**.
- UI, fixture security and local lifecycle/operation regressions: **55 passed**
  combined (including the 14 UI and 5 fixture checks).
- Workspace all-target Clippy `-D warnings`, formatting, 43-migration integrity,
  production web build and web lint: passed.

**Historical failure, resolved below.** The initial `cargo test --workspace` failed
in two then-unchanged runner connection fixture tests:
`native_fixture_personal_github_profiles_and_login_replay_survive_restart` and
`native_fixture_ready_ack_refresh_rejection_and_old_run_pins_survive_restart`.
Both fail at `crates\crony-runner\src\connection_setup\integration_tests.rs:326`
with Git `fatal: The empty string is not a valid path`; this fixture passes
`core.hooksPath=` to installed Git `2.55.0.vfs.0.8`. Runner totals were
222 passed, 2 failed, 1 ignored. Earlier workspace crates passed; Cargo stopped
before all remaining workspace crates, so no full-suite pass is claimed.
The unchanged tests were not patched or skipped to manufacture a green gate.

An initial Phoenix direct `corepack` spawn failed on Windows; a PowerShell wrapper
resolved it. An initial web build could not find nested `pnpm`; adding the
existing `output\tool-bin` shim directory to PATH resolved it. These are distinct
from the workspace test failure.

Logs: `output\delegated-evidence\workspace-tests.log`, `targeted-final.log`,
`server-final.log`, `node-final.log`, and `room-regression.log`.
No commit or hosted PR was created while the mandatory workspace gate was red.
The local PR proposal remains an **UNSENT DRAFT** outside the repository.

## Git compatibility correction and final mandatory gates

Phoenix reproduced both failures with installed Git **2.55.0.vfs.0.8**.
The rejected empty `core.hooksPath=` occurred at the real production
`NativeGitHub::git` boundary and in both native fixture helpers, not just a test.
The correction supplies an absolute, canonical `empty-hooks` directory in
runner-owned state outside the checkout. Links/reparse points and nonempty
directories fail closed. The Rust fixture reuses this helper; fake GitHub uses
its already-owned empty directory. No global Git configuration, installed Git
version, test selection, or ignore annotation was changed.

The added real-Git regression first proves default and repository-configured
`post-checkout` hooks execute with an explicit positive-control override, then
proves both fixture and production commands suppress those hooks. It also
verifies populated hook-suppression state fails closed.

Fixture-root retries are retained honestly: using artifact storage inside this
checkout correctly failed the private-state/source boundary; a long sibling
artifact root hit Git's `$GIT_DIR` path limit. The final run set process-local
`TEMP`/`TMP` to the short, owned workspace-child
`C:\Users\awiedemann\Projects\TaskRabbit\v285`, outside the source checkout.
Neither boundary was weakened.

Final Phoenix `sense` results:

| Check | Result |
| --- | --- |
| Connection suite, including both original failures and hook regression | **25 passed**, none ignored |
| Complete `cargo test --workspace` | **583 passed, 0 failed, 334 intentionally ignored**, no filters |
| Runner within workspace | **225 passed, 1 ignored** |
| Server within workspace | **145 passed, 5 ignored** |
| Store within workspace | **61 passed, 328 ignored** |
| UI/fixture/local-stack Node regressions | **55 passed**, none skipped |
| `cargo fmt --check` | Passed after correcting new regression formatting |
| `cargo clippy --workspace --all-targets -- -D warnings` | Passed |
| `node tools\check_migrations.mjs` | **43 migrations**, immutable checksums preserved |
| `corepack pnpm build:web` / `lint:web` | Both passed with existing `output\tool-bin` shims |
| Focused review of the three-file compatibility correction | No significant issues found |
| Final 46-file candidate and staged-blob privacy/whitespace checks | Passed; no private/generated paths or new credential material |

The 334 pre-existing opt-in ignores remain explicit; no new test was ignored.
The earlier explicit SQLx room-admission pass is additional to these workspace
totals. Final logs are `output\delegated-evidence\native-fixture-final.log`,
`workspace-tests-final.log`, `node-compatibility-final.log`, `format-final.log`,
`clippy-final.log`, `migrations-final.log`, `web-build-final.log`, and
`web-lint-final.log`. Earlier failure logs remain retained.

The privacy check covered five high-entropy private values, database credential
contexts and credential patterns. Two existing public development database
defaults matched the short local database password; both were verified unchanged
against `HEAD`, not misrepresented as newly leaked credentials. Private fixture,
key, runtime, `.phoenix` and generated artifacts were not staged.

Phoenix trace verified at **98 rows**, no broken entry, head
`f47560d2677aff88133b1e3325fcbd27efa819dde42dac652baca1f7ccaba8d5`;
subsequent staging/commit checks can append rows. The demo listeners on
5187/8791/54330 remained running; no login, release, reset or restart was replayed.
The final-source migration-43 versus runtime migration-42 distinction above
remains unchanged. No mandatory code-validation blocker remains.

## Scope limits

Live Azure/Entra is **NOT_EXECUTED**. The Entra implementation is offline-tested,
not deployed or live-qualified. No Azure SQL, Cosmos DB or arbitrary-resource
connector claim is made. The maintained configured connector is a bounded
protected-flag read with an independently supplied SHA-256 expectation.
Local ECorp uses development principals; production OIDC and strong OS isolation
are not established by this test. Mid-exchange server/runner reconnection has
not been qualified. The implementation and local synthetic acceptance do not
constitute full production readiness.

See `docs\DELEGATED_CONNECTIONS.md` for architecture, exact configuration,
private credential location, restart procedures and validation commands.
