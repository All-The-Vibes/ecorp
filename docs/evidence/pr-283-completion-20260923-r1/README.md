# PR #283 completion evidence

Date: September 23, 2026. Maintainer completion pass performed under the existing ECorp-only authorization.

Integrate current main while preserving migration bytes and historical evidence. Bind canonical Factory replay to retained immutable decisions, distinguish unavailable witness verification from divergence, separate checkpoint work from bounded concurrent publications, rotate checkpoint discovery, share the exact GitHub SHA grammar, prepare audit configuration before startup effects, and require EVM Revert for expected contract denials.

Nine contributor gates, 44 real PostgreSQL audit regressions, native startup nonmutation assertions, canonical-format compatibility and the local EVM contract test. An owned browser/server/runner fixture proves audited mission revisions, stable accepted/refused replay, verified completion and offline checkpoint verification. No live GitHub publication, Entra acceptance, or live-chain transaction is claimed.

The nine required commands passed on source head `2b276332e3944aa38ce1d980698180dff6caaf68` with staged tree `35f923a2e11dbf5326186be5c5e9c5c146626373`, incorporating main `08b0b89b2dd16c1906c9dd3468c5ddecca24c4ce`. `validation.json` binds the commands, tool versions, statuses and log hashes. Local user and checkout paths in the published logs are normalized; original hashes are also retained. The evidence files added here are subsequent documentation of that tested tree, not a claim that a not-yet-existing commit was executed.

`validation.png` is a genuine Edge screenshot of `validation.html`, rendered from these saved results. It is a test-report capture, not an application screen. Full result lines, including opt-in ignored tests, remain in the logs. No original RED/GREEN development chronology is inferred from retrospective validation.

Reproduction: use an isolated checkout of the PR, install the repository-pinned Rust/Node/pnpm toolchain, run `pnpm install --frozen-lockfile`, and execute the nine commands listed in `validation.json`. Set `RUST_TEST_THREADS=1` for the recorded Windows test configuration. Build the repository's native `crony-mcp` binary and set `CRONY_MCP_TEST_BINARY` to that owned binary for native MCP unit coverage. These results do not substitute for fresh hosted checks, required independent reviews, or production identity/provider acceptance.

Maintainer self-review: inspected the final implementation and its current-main integration against the product, architecture, security and evaluation contracts. Only evidence documented here is claimed; existing historical reports retain their original scope and limitations.

Native Git received `core.longpaths=true` through process-only Git configuration for the recorded commands. Repository and global Git configuration were not changed.

Build provenance: Workspace package artifacts are removed under the task Cargo mutex before Rust validation. Third-party dependency cache is retained. Node tests use a freshly built copied native MCP binary. `validation.json` also binds the workspace-refresh and MCP-build logs, their original/published hashes, and the copied MCP binary hash.


The 44 native PostgreSQL cases (9 server, 35 store) and real-binary startup checks ran on tree `71e638bea477547c4f1775da2941776ef9fd2062`. The only later changes before the nine-gate and browser acceptance tree `35f923a2e11dbf5326186be5c5e9c5c146626373` are the five contributor documents listed in `acceptance-provenance.json`; implementation, migrations and dependency locks are identical. This is a fresh native rerun with verifiable tree identity, not a replacement of old expected hashes. Earlier PR evidence remains historical and unchanged.

Actual Edge screenshots in `browser-r3/` show the saved mission awaiting dispatch and its completed single run with a passed verifier and downloadable evidence. The driver also verifies accepted and refused revision replay, denies a non-owner coverage request, exports three audit rows, verifies the checkpoint offline with an independently retained public key, rejects another key, and proves the configured source checkout remains clean and unchanged. Fixture data and failed attempts remain retained; cleanup stops only recorded owned processes.

New Factory keys share canonical replay identity. Accepted historical padded keys are recovered only through their exact native operation and unique resulting version. Historical padded refusals cannot be recovered from a UUID hash alone: the original spelling must establish the immutable alias once. Ambiguous historical refusals fail closed for explicit reconciliation; they are never silently relabeled or combined. Destination-bound witness configuration must use each destination's exact 40-character GitHub commit. Existing applied migration files/checksums remain unchanged; 0051 adds the replay-alias table. Other open PRs with independent migration allocations still require coordinated integration before their merge.

Maintainer self-review covers the full current contribution and current-main integration against the ECorp product, architecture, security and evaluation contracts, including tenant scope, unchanged source checkouts, verifier-backed completion, bounded/fair workers, history-compatible idempotency, typed unavailable witness outcomes, retained destination fences, and startup preparation before persistent effects. All nine commands passed. Ignored opt-in cases in the ordinary workspace run are not counted as executed SQLx coverage. Node used here is 24.21.0; the repository pin is 24.19.0. Hosted checks and protected independent approval remain separate.

Trailing line whitespace and blank lines at EOF are removed from the published logs. This formatting normalization does not alter test-result text; original and published hashes are distinct. The local `.gitattributes` preserves published evidence bytes on checkout.
