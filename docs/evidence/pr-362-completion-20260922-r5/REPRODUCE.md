Run python -X utf8 -m unittest discover -s tools -p test_public_evidence_verifier.py -v and the nine commands in validation.json on the published commit. The hard-link and real junction/symlink cases must execute; report any platform skips accurately. The recorded exact-byte driver exports the source and existing packet files from Git before execution. Use fresh task-owned paths for historical driver placeholders. All baseline failures are retrospective defect regressions, not original development chronology.
