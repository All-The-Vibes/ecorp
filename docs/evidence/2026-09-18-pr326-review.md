# PR 326 review corrections

Review began on PR [#326](https://github.com/All-The-Vibes/ecorp/pull/326) at
`d6cb1c5ea90430ed1e107a326d30597d69eaec25`. Its five inline review threads were
resolved and all 17 hosted checks passed on `755553bc3c20e8750195ab631dec1ee3f3fef725`.
The maintainer merged it at `b641497c6877438675e7a5480983d4648b89cfb1` on
2026-09-18 at 16:38:45 UTC while additional review-body advisories were being
validated. That merge has exactly the same tree as the tested PR head.

The remaining advisory corrections are a separate follow-up on
`codex/pr326-review-followup`, based on that merge. They preserve the coverage
thresholds, production key rejection, authorization and accepted-completion rules.
This report separates current local validation from earlier runtime evidence.

## Findings and corrections

| Finding | Correction and reproduction |
| --- | --- |
| Web coverage rejected the source inventory before testing. | Classify `runActivity.ts` and `missionCollaboration.ts`; report all 18 production models and derive the output count from the inventory. |
| Hosted Windows coverage selected Git's `link.exe`. | Prepend the selected Visual Studio x64 MSVC directory, check its containment and resolved linker, and retain `linker-path.txt`. The exact workflow setup compiled and ran instrumented Rust with Git's linker initially first on PATH. Missing or foreign MSVC selections rejected before compilation. |
| Production identity fixture could not become healthy. | Replace its public development key with a fresh 32-byte key in the child environment. The original fixture reproduced the health timeout; the corrected fixture passed. The server's production-key guard remains unchanged. |
| Audit status accepted state containing an unknown file. | Apply the existing directory inventory validation on reads as well as writes. The actual CLI now rejects the unknown file while retaining it and the checkpoint. |
| Exhausted audits fetched inputs before rejection. | Check the shared 100-attempt limit before snapshot, feedback or live GitHub collection. Regressions prove zero reads after exhaustion and exactly one final eligible cycle. |
| Read-only MCP still accepted access tokens on the command line. | Use Clap's native value-source metadata to reject command-line tokens before creating the client. Eight rejection cases failed against the original gateway; all 26 native probe tests passed after rebuilding. Tests cover both option spellings, both read-only mode selectors, environment-only success and unrestricted legacy compatibility. |
| Instrumented Windows fixtures exceeded native ACL setup deadlines. | Prepare the exact Windows PowerShell ACL host after instrumented compilation, verify permissions on a new owned directory, then run the complete serial workspace tests. Parallel execution failed both connection fixtures; serial execution alone still failed the first. Production deadlines, tests and the 45% line floor remain unchanged. |
| The HTML report labeled earlier test counts as the latest continuation. | Label its 1,346-test result as an earlier snapshot and separately link the 1,361-test local PR validation. Historical benchmark scores and provider evidence keep their original scope. |
| Receipt and feedback readers treated a short native read as complete. | Accumulate bounded descriptor reads while retaining size, growth and exact-byte-hash checks. Six regressions cover valid short reads and concurrent truncation/growth; both positive cases failed on the old implementation. |
| Malformed omitted-ID MCP objects could receive errors despite the notification policy. | Drop every omitted-ID object before deserialization and tool dispatch; explicit-null requests and non-object errors retain responses. Both new regressions failed against the old binary; all 28 native probe tests now pass. Clarify the policy and fix duplicated specification numbering. |
| Steward CI ran under Node 22 despite requiring Node 24. | Keep the web/tool compatibility lane on Node 22, then select the pinned `.node-version` before Steward. Require successful runtime setup before execution; native actionlint passes. |
| Startup collapsed actionable secret-key failures into one generic error. | Return typed static reasons for missing, malformed and forbidden development keys, without values or decoder causes. Three startup regressions and five actual binary cases pass. |
| Feedback review evidence could change between pathname checks and reading. | Bind one descriptor, use supported no-follow/nonblocking flags, enforce the byte bound, and compare bigint identity/revision metadata before and after reading. Seven adversarial cases pass; the old reader accepted a substituted file and the corrected reader rejects `REVIEW_CHANGED`. |
| Audit input acquisition failures disappeared from durable attempt history. | Acquire outside the writer lock, then recheck control/source/bounds and persist an admitted intent followed by the fixed `INPUT_ACQUISITION_FAILED` result. Raw exceptions are not retained. Six regressions failed before correction; an actual failure/success/failure CLI sequence retains every attempt and earlier accepted evidence. |
| Verification previews lost command argument and JSON-key boundaries. | Render exact JSON arrays, preserve repeated spaces with `pre-wrap`, and display the singular `1 byte`. Model tests distinguish one item containing separators from multiple items. Browser acceptance checks the rendered arrays and native persisted argument execution. |

The third review thread arrived after the first correction commit, `45fe402`.
Its credential-boundary correction received a fresh full contributor-gate run.
The subsequent coverage environment correction changes no executable or test
source, so those nine contributor-gate results retain the same code scope. Its
hosted coverage job is the acceptance gate for prepared, serialized instrumented
execution. The build/test split uses pinned cargo-llvm-cov's native `show-env
--pwsh`, native Cargo's `test --tests --no-run`, and ordinary test/report commands.
The readiness operation has a separate bounded setup allowance; each real fixture
still exercises the production ten-second ACL operation deadline. There is no
automatic test retry, exclusion or coverage-floor reduction.
The two audit defects were reproduced against an isolated archive of the original
PR head before their regression tests passed with the fixes. The review also
covered native MCP inspection, receipt and feedback admission, retained behavior
imports, the verifier-editor extraction, Copilot settings removal and workflow
permissions. The additional corrections above came from reading full review bodies,
including their suppressed comments, and reproducing the defects. Final reconciliation
of all eight reviews and five inline threads found no unaddressed advisory.

## Validation

Local environment: Windows, Rust 1.98.1/MSVC, pnpm 11.19.0, Node 24.19.0 for
web/tool/Steward commands. Native Rust verifier and identity fixtures used the
installed Node 22.23.2/npm 10.9.8 toolchain.

| Command | Result |
| --- | --- |
| `node tools/check_migrations.mjs` | Passed. |
| `pnpm check:docs` | Passed. |
| `pnpm test:unit` with `CRONY_MCP_TEST_BINARY` selecting the current compiled gateway | 1,157 passed; zero failures or skips; 56.749 seconds. |
| `pnpm test:steward` | 227 passed; zero failures or skips; 157.408 seconds. |
| `cargo fmt --check` | Passed. |
| `cargo clippy --workspace --all-targets -- -D warnings` | Passed; 41.874 seconds. |
| `cargo test --workspace` | 564 passed; zero failures; 343 explicitly ignored; 115.392 seconds. |
| `pnpm build:web` | Passed; 15.240 seconds. |
| `pnpm lint:web` | Passed; 3.248 seconds. |
| `pnpm coverage:web-models --output output/pr326-advisory-final-web-coverage` | 400 tests passed across 23 test files; all 18 models; unchanged source inputs. Lines 99.64%, functions 97.01%, branches 97.07%. |
| Native startup diagnostics | 5/5 actual binary cases passed; static actionable reasons, no configured values or decoder causes. |
| Native workflow lint | `actionlint .github/workflows/repository-checks.yml` passed. |
| `node --test tools/coverage_web_models.test.mjs` | 6 passed. |
| Earlier Windows instrumented build/readiness/test/report workflow on Rust inputs at `4cdfd71f89bc2fd19857b8bd8e37532a41b6f340` | 561 passed, zero failed, 343 ignored; JSON and LCOV agree on 34,579 / 74,998 lines (46.10656%), above the unchanged 45% floor. All 102 measured Rust inputs stayed unchanged; 555.814 seconds. This predates the advisory Rust changes. |
| `node tools/e2e_steward_maintenance.mjs --output <new-absolute-directory>` | 17 checks passed across 23 actual CLI invocations. Synthetic snapshots; no live GitHub writes. |
| `node tools/e2e_identity.mjs` on the owned fixture | All 16 recorded identity/fencing outcomes passed; 4.939 seconds. Original driver failed with the expected health timeout in 23.856 seconds. |
| `node tools/e2e_secrets.mjs` on the same owned fixture | Passed; scoped delivery, denied unauthorized access and no plaintext in snapshots/events/logs; 5.150 seconds. |

The first local Rust attempt retained a failure because the bundled Node PATH
lacked `npm.cmd`. The repeated workspace command uses the installed native npm
shim; no test or production code was weakened to address that prerequisite.
An initial web/tool run skipped 22 native-gateway cases before the binary was
selected. The first correction's 1,135-test run, the merged correction's 1,147-test
run and the advisory follow-up's 1,157-test run executed all their native cases,
with zero skips. The latest Node total is 1,384: 1,157 web/tool plus 227 Steward.
Times in the current gate table are full command wall times from its receipts.

The earlier full instrumented validation used a new owned target, retained
dependency cache, and native workspace cleanup before compiling new test binaries
and generating new profiles. Its exact ACL readiness check passed in 556 ms. Two
separate controls verified incorrect ACL rejection and the real 60-second startup
timeout: both exited unsuccessfully, reaped only their owned child and refused to
admit the test stage. Original targets, failed hosted results and profiles remain
retained. The measured run's summary, native JSON/LCOV, logs and source identities
are under `C:\Users\rajeshsingh\.ecorp-local\pr326-current-instrumented-736612ed\full-workspace`.

Identity/secrets acceptance used a new owned PostgreSQL 17 cluster, development
server, temporary production server, fake local OIDC issuer and native runner
against an independent Git fixture. Cleanup stopped only that stack, verified
its four ports were clear, and retained the database, worktrees and evidence.
These are deterministic native runtime checks, not real-provider inference or
production multi-host authentication.

## Browser/server/runner advisory acceptance

`node tools/e2e_verification_policy_browser.mjs` passed in 29.063 seconds against
an explicitly owned PostgreSQL/server/runner/web stack with `ECORP_POLICY_TEST=1`
and its owned `ECORP_POLICY_SETUP` receipt. Runtime: Node 22.23.2, Playwright 1.62.1,
Edge 153.0.4234.32 and PostgreSQL 17.11. The fresh native binaries include the
advisory startup fixes. The runner uses a separate synthetic source repository.

- Desktop 1440x1050 and mobile 390x844 parsed rendered command argv and JSON keys
  back to the exact authored arrays, preserved repeated spaces and showed `1 byte`.
  Keyboard focus, draft manual gates and both viewport overflow checks passed.
- Native command verification asserted arguments containing spaces, repeated spaces,
  quotes and backslashes. Persisted command/test evidence retained exact argv and exit
  code zero. Run `f6a770ed-11dc-4b91-8bf0-55aeabe162db` passed six of six checks with
  exactly one accepted completion event.
- Run `c1b59fd0-a98f-4147-b31f-5152357464cc` failed the negative artifact-floor policy
  as intended, without accepted completion. No page errors or forbidden requests occurred.
- Input hashes remained stable. The owned runner was revoked; owned web, runner,
  server and PostgreSQL processes were stopped; all three ports were clear.
  Databases, worktrees and failed/accepted evidence remain preserved.

This is deterministic `fake-process` acceptance with development identity, without
real-provider inference, production authentication or a human outcome-review decision.
The full receipt is `output/pr326-ui-policy-f3263a67/ACCEPTANCE.md`; exact policies,
persisted IDs/events and screenshot hashes are in its
`browser-2026-09-18T16-58-16.764Z-2c3f607a/report.json`.

Visually inspected browser captures contain only synthetic fixture data:
[desktop](assets/pr326-review/desktop-editor.png) and
[mobile](assets/pr326-review/mobile-editor.png).

## Retained evidence

Local receipts and full logs are retained under `output/pr326-validation/`,
`output/pr326-final-validation/`, `output/pr326-mcp-token/`,
`output/pr326-review/`, `output/pr326-identity-review.md`, and
`C:\Users\rajeshsingh\.ecorp-local\pr326-identity-20260918-e0bbf14c`.
Current advisory gate receipts are under `output/pr326-advisory-validation/` and
`output/pr326-advisory-final-validation/`; the final model coverage receipt is
`output/pr326-advisory-final-web-coverage/summary.json`. Failed attempts, differential
reproductions and diagnostic logs remain preserved. The final Node/web rerun includes
the last JSON-key summary edit; Rust and Steward inputs were unchanged after their
complete gate runs.

Hosted checks must be assessed against the follow-up commit; PR #326's successful
checks do not verify the later advisories. Prior benchmark and historical evidence
files retain their original assessed-source scope; these fixes do not rescore them.
