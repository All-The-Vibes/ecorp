# Reproduce the temporary-root regression

Use the published commit with Python in ordinary mode. Run python -X utf8 -m unittest discover -s tools -p test_public_evidence_verifier.py -v and the nine commands in validation.json. The owned junction/symlink test must execute; report permission-based skips accurately. The test mocks only allocation return, creates a real filesystem alias, checks copying against the physical root and then runs the production verifier.

The recorded exact-byte driver exports the test module and retained evidence packets from Git before execution. Replace normalized local placeholders with new owned paths if replaying that historical driver. The hosted-failure directory retains the actual preceding macOS failure. The validation image is a saved-report capture.
