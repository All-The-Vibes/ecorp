# Preserve recorded receipt bytes across native and Git checkouts.
* -text whitespace=cr-at-eol
# Native output and saved patch context retain their original whitespace.
*.log -whitespace
*.patch -whitespace
