# ECorp PR queue corrective validation

This separate packet records the corrections after the first published integration of the ECorp queue. The earlier packet at ../pr-queue-completion-20260923 is unchanged and retains its original source identities and outcomes.

This is the validation before integrating the newer PR #323 commit 592ab11c2a733579d46e990a0685524b3a6684d4. At this source, the aggregate Node command omitted the inherited 180-second default, while explicit test-specific deadlines remained active. The separate pnpm test:unit command is not included in these eleven gates. The later reconciliation packet records the restored deadline and the additional required command without relabeling these executions.

Current local validation executed against staged tree db307a6fc9768ca6caf9309d980b3e21fdd98051, a nine-file correction of published commit 4cc00784ff7a23c40d61bfce1e96152a22093f2f. Across the initial validation and one complete Node-lane retry, all eleven named gates and the native MCP rebuild passed. Exact commands, real test counts, ignored/skipped scope, original log hashes and normalized public log hashes are in validation.json.

The initial full Node run was unsuccessful: 3,030 tests passed, zero assertions failed, one native Teams authentication test was cancelled and 21 tests were skipped. SDK initialization took about 91 seconds against the original 15-second test deadline. The unchanged 18-test Teams file then passed in isolation with the same timeout and authentication assertions; SDK initialization took about 12.5 seconds. A single repeat of the complete Node lane on the same source passed 3031 tests, with 0 failed, 0 cancelled and 21 skipped. The other ten passing gates retain their original execution times and logs. The initial unsuccessful log and isolated diagnostic are preserved under validation-history/ with hashes and explicit retry provenance. The cause of the slow initialization is not established.

Fresh affected native acceptance used that same tree: worker HTTP gateway restart, recovery and finality against owned PostgreSQL and pinned local Anvil; SELECT-only database authorization with SCRAM and wrong-password rejection; and delegated browser/server/runner acceptance against synthetic Keycloak. Live Azure and production authentication were not exercised. The five native suites in the earlier packet remain historical results on their recorded execution trees.

The same source also passed all 22 focused Windows external-adapter tests with checksum-pinned cargo-llvm-cov 0.9.1, Node 22.23.2 and native MSVC instrumentation. The shutdown case that previously timed out passed. A separate instrumented execution of the complete crony-runner test binary then passed 260 tests, with zero failures and 5 ignored tests. The separate receipts and logs are in windows-instrumented.json and windows-runner.json. These diagnostics do not measure full-workspace coverage or replace the hosted Windows gate.

The corrections prevent local delegated credentials reaching configured proxies while retaining remote proxy support; distinguish macOS's unsupported invalid-UTF-8 filesystem fixtures from portable rejection tests; supply pinned Anvil to the Linux SQLx coverage lane; make the Windows cancellation test own and abort its adapter task with early-exit diagnostics; and narrowly classify historical/synthetic snapshot findings by exact path and field/value. The Windows test change does not prove the earlier hosted timeout's cause. Successful hosted coverage and native platform jobs are still required before merge.

policy-review.json binds the policy delta to trusted main 34002d3a362dd7b031c70803cf918e78f982bf1f. Proposed reviewer instructions are code under review and cannot authorize their own acceptance. codeql-review.json records individually reviewed native alert dispositions; CodeQL remains enabled.

The inherited medium glib 0.18.5 advisory RUSTSEC-2024-0429 / GHSA-wrw7-89jp-8q8g remains open in the Tauri desktop lockfile. No direct application VariantStrIter use was found; transitive unreachability and a compatible upgrade are not claimed.

Only this new packet is excluded from the comparison with the tested tree. Every original contributor head remains an ancestor through the published integration commit. Text transformations normalize local paths/usernames and line endings; original hashes and private artifacts are retained. Native public receipts contain selected factual fields and do not publish private fixture credentials or complete capability payloads.
