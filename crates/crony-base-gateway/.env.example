# Non-secret path only. The binary does not automatically load this file.
ECORP_BASE_GATEWAY_CONFIG=C:\customer\base-gateway\gateway.json
# Provision AWS workload identity through the deployment's supported SDK provider.
# Database URLs and the gateway token belong in broker-mounted secret files.
